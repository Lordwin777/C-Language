#include <stdio.h>
#include <stdbool.h>
#include <math.h>


int fun(int i, int n)
{
    if(i<n)
    {
        if(n % i == 0)
        return 0;
        fun(i+1,n);
    }
    else
    return 1;
}

int main()
{
    printf("%d",fun(2,9));
}
